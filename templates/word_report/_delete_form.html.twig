<form class="form-delete" method="post" action="{{ path('word_report_delete', {'id': word_report.id}) }}">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ word_report.id) }}">
    <button class="frame frame-important fas fa-trash-alt clickable"></button>
</form>
